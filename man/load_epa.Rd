% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_epa.R
\name{load_epa}
\alias{load_epa}
\title{Load EPA datasets}
\usage{
load_epa(prefix, suffix = NULL, path = system.file("data", package = "ineEpa"))
}
\arguments{
\item{prefix}{A character string indicating the database prefix. Valid options include:
\itemize{
\item \code{"poblacion"}: Datasets related to population statistics.
\item \code{"activos"}: Datasets for active population.
\item \code{"ocupados"}: Datasets for employed population.
\item \code{"parados"}: Datasets for unemployed population.
\item \code{"inactivos"}: Datasets for inactive population.
}}

\item{suffix}{An optional character string indicating th dataset suffix. If omitted,
the function lists all available suffixes for the specified prefix. Valid suffixes depend
on available files in the specified directory.}

\item{path}{A character string specifying the directory where the dataset files are stored.
The default is \code{"data"}.}
}
\value{
If \code{suffix} is provided, the function returns the loaded dataset as a \code{dataframe}.
If \code{suffix} if ommitted, the function returns a character vector listing all available
suffixes for the specified \code{prefix}.
}
\description{
The \code{load_epa} function provides a user-friendly interface for loading processed EPA datasets.
Users specify a \code{prefix} to identify the dataset category (e.g., "poblacion", "ocupados"),
and optionally a \code{suffix} to refine their selection (e.g., "sector", "edad"). If no \code{suffix} is
provided, the function lists all available options for the given prefix.
}
\details{
The datasets are stored as \code{.rda} files and names using a \code{prefix_suffix.rda} format.
For example, a dataset for "ocupados" by "sector" would be stored as \code{ocupados_sector.rda}.
The function automatically builds the file path based on the provided \code{prefix} and \code{suffix},
validates their correctness, and loads the corresponding dataset.

If no \code{suffix} is provided, the function scans the directory for files matching the
specified \code{prefix} and extracts the available suffixes.
}
\examples{
# List all available suffixes for the prefix "ocupados"
load_epa("activos")

# Load the dataset for "ocupados" by "sector"
data <- load_epa("activos", "edad")

# Load the dataset for "poblacion" by "edad"
data <- load_epa("activos", "formacion")

}
